Use colab or your local jupyter notebook to run this code.
cv2, numpy, matplotlib, PIL required.
If you run it locally, just ignore codes of changing directories. That means, don't run them.
If you run it on colab, you need to change the path str to your own path.
